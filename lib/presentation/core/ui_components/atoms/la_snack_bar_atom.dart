import 'package:flutter/material.dart';

class LaSnackBarAtom extends SnackBar {
  const LaSnackBarAtom({
    super.key,
    required super.content,
    super.backgroundColor,
    super.behavior,
    super.duration,
  });
}
