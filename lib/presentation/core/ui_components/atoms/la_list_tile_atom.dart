import 'package:flutter/material.dart';

class LaListTileAtom extends ListTile {
  const LaListTileAtom({
    super.key,
    super.leading,
    super.title,
    super.subtitle,
    super.trailing,
    super.onTap,
  });
}
